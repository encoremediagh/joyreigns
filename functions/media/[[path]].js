export async function onRequestGet({env,params}){if(!env.MEDIA)return new Response('Not configured',{status:503});const object=await env.MEDIA.get((params.path||[]).join('/'));if(!object)return new Response('Not found',{status:404});const headers=new Headers();object.writeHttpMetadata(headers);headers.set('etag',object.httpEtag);headers.set('cache-control','public, max-age=86400');return new Response(object.body,{headers});}
